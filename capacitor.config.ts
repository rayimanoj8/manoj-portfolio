import type { CapacitorConfig } from '@capacitor/cli';

// @ts-ignore
const config: CapacitorConfig = {
  "appId": "com.manoj.portfolio",
  "appName": "manoj-portfolio",
  "webDir": "dist"
}


export default config;
